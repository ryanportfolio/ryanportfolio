# Scratch space: labs, repro scripts, throwaway tooling (see CLAUDE.md)
.tmp/

# Per-machine Claude Code settings
.claude/settings.local.json

# Env / secrets
.env
.env.local
.env.*.local

# Fleet audit config: names private repos (incl. excluded ones); never commit
app/fleet.config.json

# Dependencies / build output
node_modules/
dist/
build/
site/dist/

# OS noise
Thumbs.db
.DS_Store

# Vercel project link (local)
.vercel/

.vercel
.env*
